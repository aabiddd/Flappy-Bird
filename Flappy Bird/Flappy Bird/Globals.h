#pragma once

// constexpr declares the varaible at compile time (for optimization purpose)
constexpr int SCREEN_WIDTH = 600, SCREEN_HEIGHT = 950;
constexpr float SCALE_FACTOR_X = 4.2f, SCALE_FACTOR_Y = 4.5f;
constexpr float BIRD_SCALE = 2.f;
constexpr float PIPE_SCALE = 2.6f;
